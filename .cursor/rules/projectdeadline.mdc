# DeadLine — Project Rules, Vision & Target Audience

**Owner:** MagistrTheOne • **Mode:** лаконично, без воды, для быстрой ориентации команды.

---

## 🎯 Vision (Зачем)

Создать премиум‑SaaS для управления проектами уровня Jira, где AI выполняет роль тимлида: ускоряет планирование, снижает риски, повышает продуктивность.

---

## 🧭 Mission (Что делаем)

* Единая платформа: доски/спринты/репорты/профили.
* AI‑оркестрация: 10+ ассистентов (GigaChat primary, OpenAI fallback).
* Перформанс и стабильность: индексы БД, кеш, failover, метрики.
* Dark+Glass UI: читаемость, фокус на данных, без визуального мусора.

---

## 👥 Target Audience (Кому)

* Стартапы и продуктовые команды 5–100 чел.
* Тимлиды/инженерные менеджеры, ценящие скорость, аналитику и минимализм.
* Компании с требованием RU‑локализации и интеграцией GigaChat.

---

## 🧱 Product Pillars (Опоры)

1. **AI‑Assistant "Василий"**: контекст задач/бордов, планирование, рекомендации.
2. **Predictive Analytics**: риски спринта, качество, загрузка.
3. **Developer Experience**: быстрые действия, хоткеи, командная палитра.
4. **Reliability**: auth везде, идемпотентность, миграции, мониторинг.

---

## 🔒 Non‑Negotiables (Правила)

* Не трогаем prod `.env*`. Секреты не логируем.
* Все API за `requireAuth()`, публичные — строго перечислены.
* POST — идемпотентные (`Idempotency-Key`). GET — `ETag/Cache-Control`.
* AI: единый клиент, **CB/RL/Cache**, версионные промпты, метрики.
* Миграции **forward‑only**, индексы — **CONCURRENTLY**.
* Логи структурные, алерты на p95, ошибки <1%.

---

## 🧩 Architecture (Коротко)

* **Frontend:** Next.js 15.5 (App Router), TypeScript, Tailwind v4, shadcn/ui.
* **Backend:** Next.js API Routes, Drizzle ORM, Neon PostgreSQL.
* **AI:** GigaChat→OpenAI (fallback), unified client, DTO `{ text, json?, model }`.
* **Realtime:** WebSocket/SSE с heartbeat и cleanup.
* **Cache:** Redis для аналитики, SWR для клиента.
* **Monitoring:** Prometheus + Grafana, structured logs.

---

## 🛠️ Dev Rules (Как работаем)

* Мелкие PR с DoD и метрикой эффекта.
* Тесты критичных путей ≥80% (401/403/429/503, AI, репозитории).
* Perf‑бюджеты в CI (Lighthouse/Web Vitals), build падает при регрессии >10%.
* Storybook/визуальные тесты для UI (по мере готовности).
* TypeScript strict mode, ESLint/Prettier обязательны.
* Dependency pinning — только фиксированные версии в package.json.

---

## 🗺️ Roadmap (Up‑next)

* **PR4:** AI Stability (Unify Client + метрики).
* **PR5:** Perf+Cache (ETag/SWR, Redis аналитики).
* **PR6:** Tests ≥80% (routes, AI, repo).
* **PR7:** Monitoring (Prometheus + Grafana, алерты).
* **PR8:** CI/CD (build/test/migrate, staging deploy, prod approvals).

---

## ✅ Definition of Success

* p95 API < 500ms, initial JS bundle (gzipped) < 2MB, LCP < 2.5s.
* AI cache‑hit ≥30%, error‑rate <1%.
* Zero‑downtime миграции, восстановление из backup проверено.
* Пользователи завершают типовые сценарии быстрее Jira (−20% времени).

---

## 🔧 Tech Stack & Versions

```json
{
  "runtime": "Node.js 20+",
  "framework": "Next.js 15.5.x",
  "language": "TypeScript 5.x",
  "styling": "Tailwind CSS v4.x",
  "ui": "shadcn/ui latest",
  "database": "PostgreSQL 16+ (Neon)",
  "orm": "Drizzle ORM latest",
  "cache": "Redis 7+",
  "ai": "GigaChat API + OpenAI API",
  "deployment": "Vercel/Railway"
}
```